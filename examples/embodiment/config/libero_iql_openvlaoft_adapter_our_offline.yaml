defaults:
  - libero_cql_openvlaoft_adapter_our_offline

runner:
  logger:
    experiment_name: "libero_iql_openvlaoft_adapter_our_offline"

algorithm:
  offline_rl:
    name: iql
    iql:
      expectile: 0.7
      weight_temp: 3.0
      max_weight: 100.0

adapter:
  add_value_head: true
